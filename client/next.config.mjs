/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special config needed for standard Next.js development.
  // The experimental turbopack.root was removed — it pointed to the parent
  // directory and caused build issues.
};

export default nextConfig;